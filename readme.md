# Godot Glow 2D No WorldEnvironment

- Version: 4.5.1
- Available: Forward+, Compability

> [!NOTE]
> This project is only focused on Forward+ rendering. 
> To use it in Compability rendering, you need to manually set `radius_scale` and `intensity` parameters in the shader, the default values are set for Forward+ rendering.
